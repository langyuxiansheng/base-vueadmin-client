/**
 * 同一导出自定义的全局组件
 */

// 卡片布局容器
// import CardContainer from './CardContainer';
// 图片显示
import ImgDialog from './ImgDialog';
import TableUtilContainer from './TableUtilContainer';
import DialogContainer from './DialogContainer';

export default {
    // CardContainer,
    ImgDialog,
    TableUtilContainer,
    DialogContainer
};